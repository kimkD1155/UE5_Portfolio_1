root = true

# 한글 주석이 깨지지 않도록 소스는 UTF-8(BOM)로 저장한다.
# MSVC 는 BOM 이 있으면 시스템 로캘과 무관하게 UTF-8 로 읽는다.
[*.{h,cpp,c,cs}]
charset = utf-8-bom
end_of_line = crlf
trim_trailing_whitespace = false
insert_final_newline = true
